---
- message:
    name: UserLookupRequest
    fields:
      - name: user_id
        type: uint32_t
        init: {0}
        description: The ID of the user to look up.
- message:
    name: UserLookupResponse
    fields:
      - name: user_id
        type: uint32_t
        init: {0}
        description: The ID of the user.
      - name: user_name
        type: char[32]
        init: {0}
        description: The name of the user.

- message:
    name: AddUserRequest
    fields:
      - name: user_name
        type: char[32]
        init: {0}
        description: The name of the user to add.
- message:
    name: AddUserResponse
    fields:
      - name: user_id
        type: uint32_t
        init: {0}
        description: The ID of the added user.

- group:
    name: Request
    members:
      - name: UserLookupRequest
      - name: AddUserRequest
        size: AddUserRequestSize

- group:
    name: Response
    members:
      - name: UserLookupResponse
        size: UserLookupResponseSize
      - name: AddUserResponse
